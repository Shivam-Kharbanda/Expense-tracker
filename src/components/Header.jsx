import React from 'react';

const Header = () => (
  <header className="w-full flex items-center justify-between bg-gray-300 p-4">
    <div className="text-lg font-bold">LOGO</div>
    <button className="bg-black text-white py-1 px-3 rounded">Back</button>
  </header>
);

export default Header;
